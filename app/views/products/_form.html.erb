<%#= form_with(model: product, local: true , :html => {:class => 'form-horizontal', role: "form"})  do |form| %>

<%= form_for @product, local: true, :html => {:class => 'form-horizontal', role: "form"} do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="form-group col-md-6">
      <div class="col-lg-10">
        <%= form.label :name, 'Nombre', class: "control-label" %>
      </div>
      <div class="col-lg-10">
        <%= form.text_field :name, class: "form-control" %>
      </div>
    </div>

    <div class="form-group col-md-6">
      <div class="col-lg-10">
        <%= form.label :description, 'Descripción', class: "control-label" %>
      </div>
      <div class="col-lg-10">
        <%= form.text_area :description, class: "form-control" %>
      </div>
    </div>

    <div class="form-group col-md-6">
      <div class="col-lg-10">
        <%= form.label :cost, 'Costo', class: "control-label" %>
      </div>
      <div class="col-lg-10">
        <%= form.number_field :cost, class: "form-control" %>
      </div>
    </div>

    <div class="form-group col-md-6">
      <div class="col-lg-10">
      <%= form.label :quantity, 'Cantidad Existente', class: "control-label" %>
      </div>
      <div class="col-lg-10">
        <%= form.number_field :quantity, class: "form-control" %>
      </div>
    </div>

    <div class="actions">
      <%= form.submit "Actualizar Producto", class: "btn btn-primary btn-xs"%> | <%= link_to 'Cancelar', products_path, class: "btn btn-info btn-xs" %>
    </div>
  </div>

<% end %>

